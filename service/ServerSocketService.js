// const WebSocket = require('ws')
// const ws = new WebSocket('ws://localhost/')
// partial
const socketSend = function () {


}

module.exports = function (req, res, next) {
  // res.socketSend(socketSend)
}
